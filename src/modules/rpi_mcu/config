#!/usr/bin/env bash
# Klipper secondary MCU on the same Pi (Linux process / RPi GPIO MCU via Klipper).
# When ratos-configuration is present, use boards/rpi/firmware.config; else CONFIG_MACH_LINUX only.
:
